{{- if .Values.secret.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "vaultplane.secretName" . }}
  labels:
    {{- include "vaultplane.labels" . | nindent 4 }}
type: Opaque
{{- with .Values.secret.data }}
stringData:
  {{- toYaml . | nindent 2 }}
{{- end }}
{{- end -}}
